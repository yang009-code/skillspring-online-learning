IMPORTANT WHEN UPDATING THE LIVE WEBSITE

The included includes/config.php contains the current course-project
database settings.

When uploading a revision, make sure includes/config.php still matches
the database information used by the live server.

Do not import sql/database.sql again unless you want to erase and
rebuild the project database.

After uploading CSS or JavaScript, use Ctrl+F5 to refresh the browser.