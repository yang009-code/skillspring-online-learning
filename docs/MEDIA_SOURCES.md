# Media Files

The SkillSpring website contains three local MP4 files:

- `media/preview1.mp4`
- `media/preview2.mp4`
- `media/preview3.mp4`

These files are used on the Course Videos page to demonstrate
HTML5 video playback.

To replace a video, use the same file name so that the PHP code
does not need to be changed.